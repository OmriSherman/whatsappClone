import React from 'react';

const ChatPage = (props) => {
    return (
        <div className="rightside">
            {(props ? 
            <div>No Chats</div>
            :
            <div>No Chats</div>
            )}
        </div>
    );
};

export default ChatPage;